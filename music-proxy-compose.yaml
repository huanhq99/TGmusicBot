version: '3.8'

services:
  music-proxy:
    build: .
    container_name: music-proxy
    restart: unless-stopped
    ports:
      - "8899:8899"
    environment:
      - PROXY_API_KEY=${PROXY_API_KEY:-your_secure_api_key_here}
      - PORT=8899
